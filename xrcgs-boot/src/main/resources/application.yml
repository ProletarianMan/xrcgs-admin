server:
  port: 9090

spring:
  application:
    name: xrcgs-admin

#Actuator确认服务在线设置
management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: "never"
